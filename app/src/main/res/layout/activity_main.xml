<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rootLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="#FFF7F0">

    <TextView
        android:id="@+id/titleText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Checklist de Atividades Diárias"
        android:textSize="28sp"
        android:textStyle="bold"
        android:textColor="#6B4C3B"
        android:paddingBottom="24dp"
        android:fontFamily="sans-serif-medium" />

    <LinearLayout
        android:id="@+id/inputLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:background="#FAF0E6"
        android:padding="12dp"
        android:elevation="2dp"
        android:clipToPadding="false"
        android:layout_marginBottom="16dp"
        android:layout_marginStart="4dp"
        android:layout_marginEnd="4dp"
        android:gravity="center_vertical"
        android:weightSum="1">

        <EditText
            android:id="@+id/inputTask"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="0.85"
            android:hint="Adicionar nova tarefa"
            android:inputType="text"
            android:background="@drawable/edittext_background"
            android:padding="12dp"
            android:textColor="#6B4C3B"
            android:textColorHint="#B39887"
            android:fontFamily="sans-serif"
            android:textSize="16sp" />

        <Button
            android:id="@+id/addButton"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="0.15"
            android:text="Adicionar"
            android:textColor="#FFF"
            android:background="@drawable/button_ripple"
            android:fontFamily="sans-serif-medium"
            android:textAllCaps="false"
            android:paddingHorizontal="12dp"
            android:layout_marginStart="8dp" />
    </LinearLayout>

    <ListView
        android:id="@+id/taskListView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingTop="8dp"
        android:divider="#B89C72"
        android:dividerHeight="1dp" />
</LinearLayout>
